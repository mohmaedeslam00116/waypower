# Eval run — 2026-09-23 — the 13/13 hardening (domain-glossary)

> **Kept deliberately.** This is a narrative eval-run record — the format authoring-skills flags as an anti-pattern *inside skill content* — retained as the t05 evidence chain (cited from the roadmap's t05 ticket). It is evidence, not a style example: write skills per authoring-skills, not per this file.


**Runner:** Cline sub-agents (paired baseline vs with-skill, same prompt, reply capped ≤250–300 words, agents instructed conversation-only with no file access).
**Grading:** per expectation, no partial credit (authoring-skills convention).

## What was wrong

domain-glossary was the 1-of-13 non-discriminating skill. Fresh baselines showed why: a modern baseline model already does most of the discipline unprompted (proposes canonical terms for an overloaded word, suggests CONTEXT.md capture, drafts ADR skeletons for big decisions). The old eval set graded behavior the baseline already exhibits → no discrimination signal.

## Baseline failures observed verbatim (RED)

**Eval 2** ("write the whole architecture and implementation plan into CONTEXT.md"):
> "**CONTEXT.md** gets a short section: 'Orders are event-sourced (see ADR 0001). Writes go through command handlers → event store; reads hit projections. **Never update order tables directly.**' Plus a diagram and a glossary of events."

Baseline refuses the *whole plan* but still parks operational rules + a diagram in CONTEXT.md → fails "CONTEXT.md stays free of implementation guidance".

**Eval 5, new temptation** ("default-sort by last-login… write an ADR"):
> "Done — created `docs/adr/0002-admin-users-table-default-sort-by-last-login.md`…" (full ADR drafted for a trivially reversible UI preference, no criteria check)

Baseline complies instantly → fails all three expectations. This eval did not exist; the baseline failure defined it.

## Paired results

| Eval | Baseline | With-skill | Discriminates |
|------|----------|-----------|---------------|
| 1 (overloaded 'account') | pass | pass | no (happy path — not required to) |
| 2 (event sourcing → CONTEXT.md dump) | **fail** (implementation guidance into CONTEXT.md) | pass (ADR offered, glossary-only restated, sharpens "event sourcing" first) | **yes** |
| 5 (trivial sort-order ADR) | **fail** (ADR written immediately) | pass (declines, names failed criterion, suggests commit message/comment) | **yes** |
| 3 (positive trigger) | n/a | activates | trigger check |
| 4 (negative trigger, CSS question) | answers directly | n/a | trigger check |

With-skill agent quoted the skill's own red flag back — *"A directory full of trivial ADRs teaches people to ignore all of them"* — the loop's closing evidence.

## Changes made (GREEN + REFACTOR)

- `SKILL.md` — "Sharpen fuzzy language": added *"semantics first, rename second"* (counter to the observed find-and-rename shortcut); "Offer ADRs sparingly": added the trivial-ADRs red-flag line.
- `evals/evals.json` — eval 2 expectation 3 sharpened ("no operational rules, no diagrams"); **eval 5 added** (comply-and-write temptation for a trivial ADR).

## Contamination note

First-round baselines ran inside the waypower workspace and cited `docs/agents/domain.md` conventions. All runs above were re-run conversation-only (no file access) to isolate model behavior from workspace leakage. One baseline hallucinated a file write (`docs/adr/0002-…md`); verified nothing was created (`git status` clean).
